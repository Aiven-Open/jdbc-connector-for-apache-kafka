Summary: Kafka Connect connector for JDBC-compatible databases
Name: confluent-kafka-connect-jdbc
Version: ##RPMVERSION##
Release: ##RPMRELEASE##
Group: Applications/Internet
License: Apache (v2)
Source0: confluent-kafka-connect-jdbc-%{version}.tar.gz
URL: http://confluent.io
BuildRoot: %{_tmppath}/%{name}-%{version}-root
Vendor: Confluent, Inc.
Packager: Confluent Packaging <packages@confluent.io>
BuildArch: noarch

Requires: confluent-common

%description

Kafka Connect JDBC is a Kafka Connector for loading data from any JDBC-compatible
 database.

%define __jar_repack %{nil}

%pre

exit 0

%post

%preun

%postun

%prep

%setup

%build

%install

# Clean out any previous builds not on slash
[ "%{buildroot}" != "/" ] && %{__rm} -rf %{buildroot}

%{__mkdir_p} %{buildroot}
%{__cp} -R * %{buildroot}
# The spec file gets included, get rid of it
%{__rm} %{buildroot}/confluent-kafka-connect-jdbc.spec

%files
%defattr(-,root,root)

%doc
/usr/share/java/kafka-connect-jdbc
/usr/share/doc/kafka-connect-jdbc

%config(noreplace) /etc/kafka-connect-jdbc

%clean
#used to cleanup things outside the build area and possibly inside.
[ "%{buildroot}" != "/" ] && %{__rm} -rf %{buildroot}

%changelog
* Wed Nov 11 2015 Confluent Packaging <packages@confluent.io>
- Initial import
